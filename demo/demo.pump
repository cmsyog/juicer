<ul style="background-color: #4ab137">
    {@each list as it,k}
    <li>${it.name} ${it.age}(index: ${k})</li>
    {@/each}
</ul>

<ul>
    {@each maps as it,k}
    <li>${it.name} ${it.age}</li>
    {@/each}
</ul>

<div>
    ${page}
</div>